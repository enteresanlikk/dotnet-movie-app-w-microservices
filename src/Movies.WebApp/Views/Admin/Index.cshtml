@model UserInfoViewModel
@{
    ViewData["Title"] = "Admin";
}

<h1>Admin</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                Claim Type
            </th>
            <th>
                Claim Value
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.UserInfoDictionary)
        {
            <tr>
                <td>
                    @Html.DisplayFor(_item => item.Key)
                </td>
                <td>
                    @Html.DisplayFor(_item => item.Value)
                </td>
            </tr>
        }
    </tbody>
</table>